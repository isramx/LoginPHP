services:
  web:
    build: .
    container_name: loginphp_web
    ports:
      - "8080:80"
    depends_on:
      - mysql

  mysql:
    image: mysql:8
    container_name: loginphp_mysql
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: phplogin
    ports:
      - "3307:3306"